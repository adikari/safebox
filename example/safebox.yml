service: safebox
provider: ssm

config:
  defaults:
    DB_NAME: "database name"
    API_ENDPOINT: "endpoint"

  prod:
    DB_NAME: "production db name"

  dev:
    DB_NAME: "dev db name"

  shared:
    SHARED_KEY: "shared key"


